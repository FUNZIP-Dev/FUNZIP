import { styled } from "styled-components";

export const OrderCategoryWrapper = styled.section`
  margin-top: 75px;
`;

export const Title = styled.h1`
  ${({ theme }) => theme.fonts.tutorial_head}
`;
